interface:
  display_name: "MyPlanner"
  short_description: "Operate MyPlanner boards, agenda, items, and subtasks"
  default_prompt: "Use MyPlanner for this task. Use the supplied token for the first useful API request; call GET /me only if identity information is needed."

policy:
  allow_implicit_invocation: true
